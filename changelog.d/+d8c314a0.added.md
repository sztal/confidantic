Added `BaseConfig.model_factory()` for deriving a subclass whose selected fields
use generated `FactoryConfig` annotations and default instances. Selectors may
inspect either field names or field names together with their Pydantic field
information. Annotation metadata is cleared by default and can be retained with
`clear_metadata=False`. Generated subclasses use an independent copy of the
default `BaseConfig.model_config`.
