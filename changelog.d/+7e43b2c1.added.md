Added `FactoryConfig` for generating configuration models from annotated
constructors, materializing target instances, and recursively resolving factory
fields through `BaseConfig.model_resolve`. Pydantic fields annotated with
`FactoryConfig` or a concrete subclass now convert target instances into
generated factory configs automatically. Materialization disables CLI parsing
for configuration models constructed during resolution or target creation.
