Added opt-in model import strings to `BaseConfig.model_dump()` and
`BaseConfig.model_dump_json()` through `context={"model_string": True}`, plus
polymorphic `model_validate()` and `model_validate_json()` for marked subclasses.
Added `BaseConfig.model_dump_yaml()` and `BaseConfig.model_dump_toml()` with
optional PyYAML and tomli-w dependencies.
Added `BaseConfig.model_validate_yaml()` and `BaseConfig.model_validate_toml()`.
