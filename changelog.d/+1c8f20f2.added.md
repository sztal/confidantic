Added `BaseConfig.model_info` and `BaseConfig.info` for returning, rendering, or
printing Rich tables. The model view summarizes types and defaults, while the
instance view shows current values with optional types. Both support optional
descriptions and opt-in color output.
