Added `BaseConfig.model_info` and `BaseConfig.info` for returning, rendering, or
printing Rich tables that summarize configuration options, types, defaults,
descriptions, and current instance values, with opt-in color output.
